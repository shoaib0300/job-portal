# Optional Bright Data Web Unlocker: company career sites (Mercedes/BMW-style) and SERP fallback.
# Indeed / Glassdoor / StepStone / XING use VPS-native scrapers by default (no token needed).
# Set JOBS_SERP_FALLBACK=1 to also run Google site-search for those boards via Bright Data.
BRIGHT_DATA_API_TOKEN=
BRIGHT_DATA_ZONE=web_unlocker1
# JOBS_SERP_FALLBACK=0
# Python for LinkedIn / Indeed / Glassdoor JobSpy (default: python3 with jobspy installed).
# JOBSPY_PYTHON=/usr/bin/python3

# Job index: fill via HTTP trigger (easy manual / external cron):
#   https://yoursite/cron/jobs-ingest?key=...
# Or: ddev jobs-ingest / bin/jobs_ingest.php. Set key below or in Super Admin → Jobs.
JOBS_INGEST_KEY=12dadbcb93cbc938c2799c205670a20

# Optional Adzuna.de jobs API (free key: https://developer.adzuna.com/). HTML search is CloudFront-blocked.
ADZUNA_APP_ID=
ADZUNA_APP_KEY=

# German PDF: DeepL Free/Pro if DEEPL_API_KEY is set (keys ending :fx use api-free.deepl.com).
# Otherwise local LibreTranslate (ddev restart). No LibreTranslate.com key needed.
DEEPL_API_KEY=
LIBRETRANSLATE_URL=http://libretranslate:5000
LIBRETRANSLATE_API_KEY=
